/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    // sArr = s.trim().split(' ')
    // return sArr[sArr.length-1].length
    return sArr = s.trim().split(' ')[sArr = s.trim().split(' ').length-1].length
};